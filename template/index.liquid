<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="preload" href="font.woff2" as="font" type="font/woff2" crossorigin="anonymous" data-next-font="size-adjust" />

  <title>Foobar</title>
  <style>
    html {
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
      -moz-print-color-adjust: exact;
    }

    body {
      font-family: ModernEra-Regular,sans-serif;
      background: #f9f9f9;
      color: #0f2c2f;
    }

    h1, h2, h3, h4, h5, h6 {
      font-family: Adieu-Bold, sans-serif;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      background: #fff;
      padding: 40px;
      border-radius: 8px;
    }

    .brand {
      color: #008272;
      font-weight: bold;
      font-size: 16px;
      letter-spacing: 1px;
      margin-bottom: 20px;
    }

    .title {
      font-size: 36px;
      font-weight: bold;
      line-height: 1.2;
      margin-bottom: 30px;
    }

    .meta {
      font-size: 14px;
      color: #555;
      margin-bottom: 40px;
    }

    .meta b {
      color: #000;
    }

    .advisor-box {
      display: flex;
      align-items: flex-start;
      flex-direction: column;
      width: fit-content;
      padding: 16px;
    }

    .avatar {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      margin-right: 12px;
    }

    .advisor-info {
      display: flex;
      margin-bottom: 4px;
      font-size: 14px;
    }

    .advisor-info strong {
      display: block;
      font-size: 14px;
      color: #00332f;
    }

    .advisor-details strong {
      padding: 8px;
    }

    .message {
      background: #f0faf8;
      border-radius: 12px;
      margin-top: 8px;
      color: #00332f;
      padding: 16px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="brand">BRAND</div>
    <div class="title">PRIVATE PENSION<br> COVERAGE SUMMARY</div>
    <div class="meta">
      Erstellt für: <b>John Doe</b> &nbsp; | &nbsp; Erstellt am: <b>05.05.2025</b>
    </div>

    <div class="advisor-box">
      <div class="advisor-info">
        <img class="avatar" src="{{ image }}" />
        <div class="advisor-details">
          <strong>{{ expert_name }}</strong>
          Expert for private pension
        </div>
      </div>
      <div class="message">
        I picked 3 out of 51 providers that fit you
      </div>
    </div>
  </div>
</body>
</html>
